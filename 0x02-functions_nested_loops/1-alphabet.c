#include "main.h"

/**
 * main - Entry point
 * Description: Print alphabets lowercase
 * Return: 0 Always
 */

void print_alphabet(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; aplh++)
	{
		_putchar(alph);
	}
	_putchar('\n');
	Return (0);
}
