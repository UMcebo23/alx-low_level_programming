#include <stdio.h>
#include <stdio.h>

/**
 * main - Entry point
 * Description: prints the lowercase alphabet in reverse
 * Return: Always 0 (Succes)
 */

int main(void)
{
	char a;

	or (a = 'z' ; a >= 'a' ; a--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
